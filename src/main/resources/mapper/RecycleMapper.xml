<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mapper.RecycleMapper">

    <!-- 对应的更新语句 -->
    <update id="recycleFile">
        UPDATE files
        SET status = 'RECYCLED'
        WHERE fileId = #{fileId} AND parentFolderId = #{parentFolderId} AND createdBy = #{userId}
    </update>

</mapper>

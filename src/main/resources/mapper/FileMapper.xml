<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mapper.FileMapper">

    <!-- 对应的插入语句 -->
    <insert id="uploadFile" parameterType="java.util.Map">
        INSERT INTO files (parentFolderId, fileType, fileName, fileSize, createdBy)
        VALUES (#{parentFolderId}, #{fileType}, #{fileName}, #{fileSize}, #{createdBy})
    </insert>

    <!--回收 -->
    <update id="recycleFile">
        UPDATE files
        SET is_recycled = 1, recycled_by = #{userId}, recycled_at = NOW()
        WHERE file_id = #{fileId} AND parent_folder_id = #{parentFolderId}
    </update>
</mapper>

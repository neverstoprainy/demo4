<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.example.demo.entity.User">
        <id column="id" jdbcType="BIGINT" property="id" />
        <result column="username" jdbcType="VARCHAR" property="username" />
        <result column="email" jdbcType="VARCHAR" property="email" />
        <result column="password" jdbcType="VARCHAR" property="password" />

    </resultMap>

    <sql id="Base_Column_List">
        id, username, email, password, userRole, userRootFolder
    </sql>

    <insert id="insertUser" parameterType="com.entity.User">
        INSERT INTO users (username, password, email, create_time, update_time, used_capacity, capacity, is_admin, avatar, folder_id, login_time)
        VALUES (#{username}, #{password}, #{email}, #{createTime}, #{updateTime}, #{usedCapacity}, #{capacity}, #{isAdmin}, #{avatar}, #{folderId}, #{loginTime})
    </insert>
</mapper>
